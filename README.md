### File Stucture

## data/
# contains counterfact.json and counterfact_plus_static.json alongisde the script which generated the plus data.

## py_env/
# contains the python virtual environment, alongside the needed pip installs